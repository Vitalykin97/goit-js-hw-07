document.addEventListener("DOMContentLoaded", function () {
  const gallery = new SimpleLightbox(".gallery a");
});

document.getElementById('galleryContainer').insertAdjacentHTML('beforeend', galleryItems);